# TempleOS Module System

## The App Contract
Every app is a module with a default export shaped exactly like this:

```js
export default {
id: 'terminal', // matches the folder name
title: 'TERMINAL.EXE', // window title bar text
icon: 'assets/images/terminal.png',
width: 640,
height: 480,
resizable: true,

// Called when a window is opened. `root` is an empty <div> inside the window body.
mount(root, ctx) {},

// Called when the window closes. Must remove every timer, interval,
// requestAnimationFrame loop, and listener attached to window/document.
unmount() {}
};
```

## The ctx API
`ctx` is the only channel between an app and the rest of the system. An app must never import from kernel/, never touch document.body or window globals belonging to other apps, and never reach into another app's DOM.

```js
ctx.fs.read(path) // -> Promise<Blob|string|null>
ctx.fs.write(path, data) // -> Promise<void>
ctx.fs.list(dir) // -> Promise<string[]>
ctx.fs.remove(path) // -> Promise<void>
ctx.save(key, value) // -> Promise<void> app-scoped settings/progress
ctx.load(key) // -> Promise<any>
ctx.openWindow(appId) // launch another app
ctx.close() // close this app's own window
```

## CSS Variables from theme.css
Not all extracted yet, but typically `#FFFFFF`, `#AAAAAA`, `#555555`, `#FFFF55` etc. (Standard 16-color CGA/VGA palette).

## How to add a new app
1. Create `apps/<id>/index.js` obeying the contract.
2. (Optional) Create `apps/<id>/style.css` if it needs specific styles.
3. Add `<id>` to `kernel/registry.js`.
4. Update this `CLAUDE.md` with the new app description.

## Apps
- `placeholder`: `apps/placeholder/index.js` - A trivial app to test the window manager.
- `standbattle`: `apps/standbattle/index.js` - Stand Battle Arena, a JoJo's Bizarre Adventure roguelike combat prototype (see `docs/stand-battle-arena-spec.md`). Playable Jotaro Kujo/Star Platinum vs. Morioh enemies and boss Yoshikage Kira/Killer Queen, across a 6-node Act 1 (Morioh) map. Implements the spec's Prototype milestone (§15 step 1): telegraphed enemy attacks, dodge (i-frames) vs. parry (tight counter window) as distinct mechanics, hit-stop/screen-shake/particle juice with a shake accessibility toggle, an effect-hook dispatcher (`hooks.js`), and a shared enemy attack-pattern module library (`ai.js`). Zero meta-progression by design; internal 480×270 canvas on a 720×260 belt plane (x, z) with a tracking camera, integer-only upscale.
  **Combat engine hardening (tech audit §1.2, Phase 0 "unblock"):** dodge (Step) is edge-triggered — one activation per physical key-down, never re-fired while a key is held — and gated by a 2-charge meter (`fighter.js`'s `DODGE_CHARGE_MAX`, 1.4s recharge each, GDD §3.7) with a HUD pip readout, closing the old "hold Space for ~77% invulnerability" exploit. All action inputs (light/medium/heavy/special/rush/dodge/parry) that arrive while the player isn't idle are queued in a 9-frame input buffer (`combat.js`'s `INPUT_BUFFER_FRAMES`) and fire the instant the player returns to idle, instead of being silently dropped. Arena world bounds (`ARENA_MIN`/`ARENA_MAX`/`WORLD_W`, plus the Phase 1 depth bounds below) are centralized in `arena_bounds.js`, imported by both the sim (`combat.js`) and the camera (`render.js`), so the two can no longer drift apart.
  **Simulation core (tech §5 Phase 1):** the sim now steps in whole frames at a fixed 60Hz rate instead of being driven by rAF's variable ms delta, on a real (x, z) belt plane instead of one lane.
  - `sim_loop.js` — `createFixedStepLoop(stepFn)`: a headless accumulator that turns variable real-elapsed ms into zero or more whole-frame `stepFn` calls, dropping backlog past 8 frames instead of spiraling. No canvas/DOM/rAF reference anywhere in it. `combat.js`'s `combat.update(dtMs)` (real usage, index.js's rAF loop) and `combat.step()` (headless, one frame, no wall clock) both drive the same per-frame `stepFrame`.
  - `fighter.js` — the entity/component store (tech §2.3). A "fighter" *is* an entity: `combat.entities = [player, enemy]` is the arena's real entity list (not two hardcoded variables), with `combat.player`/`combat.enemy` kept as named references into it for the render/pose/HUD/audio code this phase doesn't touch. Components are flattened onto the entity object rather than nested (`.x`/`.z` for Transform, `.hp`/`.maxHp` for Health, etc.) because pose_player.js/pose_enemy.js/render.js/hud.js already read those fields directly and this phase doesn't rewrite them. `.body`/`.poise`/`.statuses`/`.standLink`/`.frames`/`.aggro` are stubs Phase 2+ fills in; `.brain` aliases `.ai` (enemies) or is `null` (the player, human-controlled).
  - `render_adapter.js` — the only new render-layer logic (deliverable 4): `zToYOffset(z)` (projection constant `Z_TO_Y_SCALE = 0.4`, lives here rather than in constants.js because the sim must never know about pixels), `depthSort(entities)` (farthest z drawn first/behind, nearest last/in front, x-tiebreak matches the pre-Phase-1 ordering exactly when z is tied), and `cameraTargetX(entities)` (midpoint of the extremes, generalizes the old two-fighter midpoint to N entities). `render.js`/`arena.js` call these instead of computing depth inline anywhere.
  - Depth movement: `input.js` maps `forward`/`back` (W/S, held-triggered like left/right) to the player's z axis; z is clamped to `ARENA_Z_MIN`/`ARENA_Z_MAX` in `arena_bounds.js` and defaults to `Z_REST` (the belt-plane's resting depth, where `zToYOffset` is 0) for every entity, so a fight where nobody touches z renders exactly as it did before z existed. The enemy does not move in z yet (no AI depth logic this phase) and hit detection is still x-only — Phase 1 explicitly excludes hitboxes/frame-data/crowd combat (tech §5 Phase 2).
  - `combat.js`/`combat_enemy.js` — every timer (attack windup/active/recover, dodge, parry, i-frames, hit-stop-adjacent state, AI windup/active/recover, projectile life, phase-transition invulnerability, hurt-flash fade, banner) is now a whole frame count at the sim's 60Hz, not milliseconds; `data.js`'s `MOVES` and `ai.js`'s `PATTERNS` were converted the same way (`windupFrames`/`activeFrames`/`recoverFrames`). combat.js split enemy-side stepping into `combat_enemy.js` to stay under the 300-line file cap. `juice.js` (hit-stop/shake/particles) deliberately stays on the real render clock — see the Phase 1 report for why that's a flagged, not silently resolved, tension with "timing is frames, never ms".
  - `headless_harness.js` — `runHeadlessFight()` builds a combat instance exactly like index.js and steps it via `combat.step()` with no canvas anywhere in the process; run standalone with `node apps/standbattle/headless_harness.js`. Same seed -> byte-identical output, proving the sim is headless and reproducible. This is where the tech §4 fairness-assertion CI suite will run once the content registry it checks exists (Phase 2+).
  - Forced, minimal touches to the pose system (normally off-limits): `pose_player.js`/`pose_enemy.js` read the same move/pattern timing fields directly for animation-progress math, so their `windupMs`/`activeMs`/`recoverMs` reads were renamed to `windupFrames`/`activeFrames`/`recoverFrames` (pure rename, zero logic change — the progress ratio is unit-agnostic) and the enemy death-pose divisor now reads the shared `DEATH_ANIM_FRAMES` constant (`constants.js`) instead of a hardcoded `900`.
  **The combat resolver (tech §2.4/§2.5, GDD §3.6-3.9, Phase 2):** frame data, real AABB hitboxes, and the three-tool defensive triangle, replacing `{windupMs,activeMs,recoverMs}` + `Math.abs(dx) <= range` for good.
  - `moves.js` — player moves as tech §2.4 timelines: `frames` total, `hitboxes[]` with absolute `from`/`to` frame numbers (multi-hit moves list several windows — barrage 4, Rush 9 — instead of dividing `activeFrames` by hit count), `cancels[]` (data-driven cancel windows, `maxSelfChain` caps Light's self-chain at 3), `armor` (Heavy only, absorbs one non-Heavy hit from frame 8). Every hitbox's `x`/`w` is `null` in the data — reach is never hardcoded per move (deliverable 1). `data.js` re-exports `MOVES` from here so existing call sites are unchanged.
  - `resolvers.js` — the five required choke points (`resolveMoveFrames`, `resolvePatternFrames`, `resolveDamage`, `applyHit`, `rollCrit`, `resolvePoiseDamage`) plus the two private helpers they share (`resolveReach`, `resolveMomentumMult`). `resolveMoveFrames(entity, moveId)` is the *only* place a move's reach is computed: `REACH_PER_RANGE (31) * stand.stats.range * move.reachMult`, and it derives the legacy `windupFrames`/`activeFrames`/`recoverFrames`/`hitCount` fields so pose_player.js/hud.js (off-limits, untouched) keep reading exactly what they always did. `rollCrit` draws from `rng.stream('combat')` (rng.js, reserved since Phase 0, now actually used) — Precision → crit chance, ×1.5 on a crit. No other file in the sim does arithmetic on a stat (invariant 5).
  - `hitbox.js` — AABB overlap in (x, z) with `depthToleranceFor(tags)`: melee ±22, ranged/wall-type exact (10) per GDD §3.5. `stepMoveHitboxes()` tracks a per-activation `spent` Set of hitbox indices so a window connects at most once regardless of how many frames it's checked across; `pointOverlaps()` is the projectile-vs-hurtbox equivalent, now depth-checked too (projectiles carry `z`, previously x-only).
  - `poise.js` — real per-enemy poise (`ENEMIES`/`BOSS_KILLER_QUEEN` now carry a `poise` stat, `data.js`), regenerating 1.2s (72f) after the last poise-damaging hit; 0 poise forces a 36f Stagger (×1.5 damage taken) through `ai.js`'s `enemyIsVulnerableToStagger()` — exported since Phase 0/1, called for the first time here — gated by the pattern's `armor` flag so an armored windup (Slam, Sheer Heart Attack) can't be poise-interrupted.
  - `resources.js` — Persistence and Momentum (GDD §3.8) as the *mechanical* resources: Momentum +8/hit +25/Clash +15/kill, decays 15/s after 1.5s without a landed hit, halves (never zeroes) on being hit, and feeds `resolveMomentumMult` (up to +40% damage, gates Stand Rush at 100). `player.comboCount` is left alone as a pure flavour input for the pose/fx/audio layer (those files are off-limits or not worth the diff) — Momentum is the number that now actually does something, per the GDD's own note that the old combo counter should have been this all along.
  - `defense.js` — Step/Guard/Clash, structurally distinct per spec §2.3. Step: 3f pre / 10f invulnerable / 6f recovery, still the Phase 0/1 two-charge 1.4s-recharge meter, just retimed. Guard (new: `keys.guard`, `KeyG` in `input.js`'s `DEFAULT_KEYMAP`/`HELD_ACTIONS`): hold, −70% damage (rest is chip), 14 Persistence/sec drain, broken by any hit tagged `heavy` or by hitting 0 Persistence into a 40f Stagger. Clash: active frames 2-8 of an 8-frame timer, 20f whiff recovery; success staggers the attacker 24f and pays +25 Momentum/+20 Persistence/90ms hitstop; a Perfect Clash (frames 2-3) refunds a Step charge and sets the attacker's `breakActive` flag (next hit ×1.8, consumed by `resolvers.js`'s `resolveDamage`/`applyHit`) — a single flag standing in for the real status system Phase 2 explicitly excludes building.
  - `combat_player.js` (split out of `combat.js`, tech §2.3's player/enemy seam) — movement, the defensive-triangle dispatch (`resolveIncomingAttack`, called by `combat_enemy.js` for both melee patterns and projectiles), and frame-data attack resolution: `player.moveFrame` advances every sim frame independent of `player.stateTimer` (kept only because pose_player.js reads it for animation progress), and `tryCancel()` checks the active move's `cancels[]` against whatever's buffered every frame — Light→Light→Light→Medium→Heavy is emergent from that data, never an authored combo list. The 9-frame buffer (`combat.js`'s `INPUT_BUFFER_FRAMES`) now fires the instant a cancel window opens, not only at full idle.
  - `ai.js`'s `PATTERNS` gained `hitbox` (melee AABB rect, checked once via `hitbox.js`'s `overlaps()` the instant the active window opens — enemy attacks are still single-shot, not frame-window-scanned, since none of the four launch patterns are multi-hit), `glyph` (`ring`|`chevron`|`crosshair`, GDD §21 colour-blind accessibility — drawn by `arena.js`'s `telegraph()`, the sim never reads it), `armor` (bool, gates poise.js's Stagger), and `tags` (`heavy` triggers Guard-break). `enterStagger()` is the shared entry point poise.js and defense.js's Clash success both use.
  - `debug_overlay.js` — hitboxes/hurtboxes/current frame/active windows/poise/i-frames, toggled by a `DEBUG` button next to `SHAKE` in the appbar (`index.js`), drawn by one additive call at the end of `render.js`'s `drawCombat` (`combat.debug`). Read-only, same rule as `arena.js`/`juice.js`.
  - `fairness_check.js` — `node apps/standbattle/fairness_check.js` asserts every `PATTERNS` entry's telegraph is ≥260ms (spec §5.1/GDD §6.8); all four already clear it (317-700ms) with room for future Menace shrinkage. Exports `checkTelegraphFairness()` for the real content-registry test suite once it exists (tech §2.9/§4).
  - Known gap, flagged not silently resolved: `pose_player.js` has no dedicated pose for the new `guard`/`staggered` (guard-break) player states — both render as plain idle since that file is off-limits this phase and neither state was an explicit deliverable. Functionally correct (drain/damage/lockout all work), just no bespoke stance yet.
  **The pipeline (tech §2.1/§2.2/§2.6/§2.9, GDD §6.7, Phase 3):** the load-bearing refactor that turns the Phase 0 notification bus into a real effect pipeline, wires the four still-dead stats for real, adds a generic status system, and builds the content validator every future Fragment/Relic/donor pool answers to. No new gameplay this phase — the acceptance test was two throwaway Fragments working as pure data with zero engine edits (verified, then deleted per the phase brief).
  - `hooks.js` — rewritten as one flat name→kind registry (`EVENT_HOOKS`/`EFFECT_HOOKS`/`QUERY_HOOKS`) instead of a single notification list. `bus.on(name, fn)`/`bus.fire(name, payload)` are unchanged for EVENT-kind hooks (`onHit`, `onDodgeSuccess`, `onParrySuccess`, `onMoveDenied`, `onTelegraphStart`, `onPhaseTransition`, plus reserved `onRunStart`/`onFloorStart`/`onNodeClear`/`onRunEnd`). `bus.effect(name, priority, fn, source)`/`dispatcher.runEffect(name, ctx)` are new: `ctx` is a single mutable object every registered effect can read/write in `priority` order (`PRIORITY.ADD < .MULTIPLY < .CLAMP` bands), any effect may set `ctx.cancelled` to short-circuit the rest, and every effect runs with `ctx.source` set to the id that registered it. EFFECT hooks: `onMoveStart`, `onHitResolve`, `onHitLanded`, `onCritCheck`, `onKill`, `onDamageIncoming`, `onDamageTaken`, `onStaggerStart`, `onStepStart`, `onClashSuccess`, `onPerfectClash`, `onGuardBreak`. `bus.query(name, priority, fn, source)`/`dispatcher.runQuery(name, value, ctx)` chain pure `(value, ctx) => value` reducers for derived numbers: `getDamage`, `getMoveFrames`, `getPoiseDamage`, `getPersistenceCost`, `getMaxPersistence`, `getMoveSpeed`. Critically, `bus.on(name, fn)` also works on an EFFECT-kind hook — it fires once, read-only, with the final resolved `ctx`, right after every registered effect has run — which is why `audio.js`/`fx.js` needed **zero changes** even though `onKill`/`onDamageTaken`/`onPerfectClash` moved from EVENT to EFFECT kind this phase. A hook name not in the registry throws immediately from any of the three registration APIs.
  - `stats.js` — the stat pipeline (tech §2.2), distinct from the hooks.js query pipeline: `createStatPipeline()` layers `base -> flat adds -> multiplicative -> set/override -> clamp` per named stat, cached and invalidated only when a modifier is added/removed. Wires the four stats the tech audit flagged dead: Range → `resolveTetherPx`/`resolveFeedbackPct` (`26*range`, `clamp(0.70-0.065*range, 0.10, 0.70)` — computed for real, unconsumed until Phase 4 builds the tether, per that phase's explicit scope), Speed → `resolveSpeedScalar` (a frame-timing multiplier `resolvers.js`'s `resolveMoveFrames` applies to every move's timeline; pinned to exactly 1.0 at Star Platinum's Speed 7 so today's feel is bit-for-bit unchanged), Precision → `resolveCritChance` (same clamp(precision*0.02, 0, 0.35) `rollCrit` always used, now cached through the pipeline) and `resolveStatusPotency` (stack-count multiplier applied in `combat_player.js` when a landed hit's queued statuses are applied; pinned to 1.0 at Precision 6), devPotential → `resolveUpgradeSlotCount` (computed, reserved for the Fragment-upgrade system, unconsumed — same "compute now, consume later" pattern as tetherPx).
  - `status.js` — the generic status system (tech §2.6, GDD §3.10): `{ id, stacks, timer, tickTimer, hitsTaken }` instances on `entity.statuses` (a fighter.js stub since Phase 1, real from this phase), definitions carry `stackRule` (`'stack'` sums and refreshes duration, `'refresh'` clamps to `maxStacks` and refreshes, `'independent'` never merges), `tickRateFrames`/`durationFrames`, and `onTick`/`onExpire`/`onDeath` hooks the engine calls generically — `stepStatuses(entity)` runs every sim frame per entity from `combat.js`. Two proof entries in `STATUS_DEFS`: `virus` (3 dmg/s/stack, real DoT via `onTick`) and `frozen` (data-only: `breaksOnHits`/`damageTakenMult`/`frozenSolid` fields exist and are correct, but nothing in the shipped game applies Frozen yet, so nothing reads them — flagged, not silently dropped). Virus's GDD-specified on-death stack-spread is a documented no-op (`onDeath(){}`) since there's no crowd/multi-enemy system yet to spread *to* (Phase 4).
  - `effect_lib.js` — the small, generic, string-addressable verb vocabulary content authors reference by name instead of writing code (spec §0's "stop and build the generic mechanism"): `EFFECT_LIB.grantResource`/`.chainedDamageMult`/`.chainedApplyStatus`/`.applyStatusUnconditional`/`.cancel`, `QUERY_LIB.multiplyIfPlayerAttacker`/`.addFlat`/`.multiplyFlat`. `installRunBuffs(dispatcher, buffs)` installs `data.js`'s `RUN_BUFFS[].queries[]` onto the dispatcher — this is how the three prototype run buffs (+15% Power, +20 max Persistence, +12% move speed) are ported off three bespoke `fighter.js` fields (`powerMult`/`speedMult`/a `maxPersistenceBonus` reduce — all deleted) onto `getDamage`/`getMaxPersistence`/`getMoveSpeed` queries, with **zero buff-specific code anywhere in the engine**.
  - `content_registry.js` — `createContentRegistry()` collects raw Fragment/Relic data (`registerFragment`/`registerRelic`/`registerDonor`); no real content exists yet (Phase 4+), so this registry is empty in the shipped game and `combat.js` still calls `loadContent()` on it every fight so the plumbing is live, not just importable. `validateContent(registry, dispatcher)` checks, across the *whole* registry before touching the dispatcher at all so every problem is collected at once (not just the first): hook names exist and are EFFECT-kind, effect `fn` names resolve in `EFFECT_LIB`, referenced statuses resolve in `STATUS_DEFS`, tags are in the `TAGS` vocabulary (`time`/`virus`/`heal`/`mobility`/`crowd`/`single-target`/`economy`/`risk`/`projection`, GDD §6.6 — distinct from the unrelated move/hitbox tag set in moves.js/ai.js), slots are in `SLOTS` (GDD §6.1's nine), donors resolve, and any Relic tagged `'risk'` has a non-null `tradeoff` string (spec §6.3). Also re-runs `fairness_check.js`'s telegraph floor as part of the same load-time gate. `loadContent()` throws one `Error` listing every problem, or (only if clean) installs each effect via `EFFECT_LIB` attributed to the content's own id. `content_check.js` (`node apps/standbattle/content_check.js`, same runnable-standalone pattern as `fairness_check.js`) is the permanent regression test: a deliberately-broken fixture proves every rule fires and all problems list together; a clean fixture proves zero false positives.
  - `resolvers.js` — every choke point now optionally takes `bus`/`stats` and runs the pipeline through itself, never around it (invariant 5 held exactly): `resolveMoveFrames(entity, moveId, stats, bus)` applies the Speed scalar then the `getMoveFrames` query; `resolveDamage(ctx)` runs `onHitResolve` (mutable: `damage`, `statuses[]`, `cancelled`, plus `slot`/`chainCount` read off `ctx.move` for chain-gated Fragments) then the `getDamage` query; `rollCrit(ctx)` runs `onCritCheck` before the roll; `resolvePoiseDamage(ctx)` runs `getPoiseDamage`; the new `resolvePersistenceCost(ctx)` runs `getPersistenceCost` (replacing `combat_player.js` reading `move.costs.persistence` directly, which would have been exactly the inline-arithmetic invariant 5 forbids).
  - `combat_player.js`/`combat_defense.js` (the latter split out of the former this phase — hook wiring pushed it past the 300-line cap, the same reason `combat_enemy.js` exists) — every hook fires from real state transitions: `onMoveStart` (attemptMove, after costs are checked-affordable but before anything is spent, so a cancel never half-spends a resource), `onHitLanded` (after a hit connects, where a Fragment queues status applications distinct from `onHitResolve`'s damage-number mutation), `onKill`/`onDamageTaken` (now EFFECT-kind, fired via `runEffect` instead of `fire`), `onDamageIncoming` (a second pass specific to damage about to hit the *player*), `onStepStart` (gated on `defense.startStep`'s own charge check succeeding), `onClashSuccess`/`onPerfectClash` (every successful Clash fires the former, a Perfect Clash additionally the latter — this is the hook the phase's "Perfect Clash refunds 25 Persistence" proof Fragment targeted), `onStaggerStart` (fired from both the poise-break path — `poise.js`'s `stepPoise()` now returns `true` the one frame it enters Stagger, so `combat_enemy.js` can fire the hook without `poise.js` needing to know the dispatcher exists — and the Clash-stagger path), `onGuardBreak` (both the Heavy-hit and 0-Persistence guard-break paths).
  - Zero visible gameplay change: `headless_harness.js` run against the same seed before and after this phase's diff produces byte-identical output (player/enemy hp, x, z, frames run, outcome). Verified by stashing the diff and diffing the harness's JSON output both ways.
  **User and Stand (GDD §3.1-3.4, Phase 4 — the core mechanic):** the Stand becomes a real second entity sharing the User's one HP pool, connected by a leash. Everything before this phase was scaffolding for it.
  - `fighter.js`'s `createStandFighter(owner)` — the Stand: `kind: 'stand'`, its own Transform (`x`/`z`)/Body/Frames (fighter.js already reserved these per-entity since Phase 1), no Health component (GDD §3.1: "All HP lives on the User. There is no separate Stand health bar."). Linked both ways via the real `standLink` component (`player.standLink.stand`/`stand.standLink.owner`) — a stub since Phase 1, real from this phase. `combat.js`'s `combat.entities` is now `[player, stand, enemy]`; `combat.stand` is a named reference into it, same pattern as `combat.player`/`combat.enemy`.
  - `combat_stand.js` (new) — the third "who does what" seam alongside `combat_player.js`/`combat_enemy.js`. `stepStand(combat)` (called first every sim frame, combat.js) owns: Close-Range anchoring (rides `ANCHOR_OFFSET_PX` = 22px ahead of the User, recomputed fresh every frame — GDD §3.4) vs. Project (held: drives the Stand outward in the movement direction, capped at `tetherPx * 1.4` — see the phase report for why the drive isn't hard-clamped to tetherPx itself — and snaps back to the anchor instantly, no recovery, the moment the key releases or a hit staggers the Stand); the tether's Strain check (over tetherPx → `player.strained = true`, 2 Persistence/sec, `player.x`/`.z` dragged 40% of walk speed toward the Stand — GDD §3.2); the aggro-weighted target picker (`pickAttackTarget`/`pickAttackTargetPoint`, tested against both hurtboxes via `hitbox.js`'s `overlaps`/`pointOverlaps`, weighted by each entity's `.aggro` — real since this phase, fighter.js's stub default 1 — when both overlap, using the deterministic `'ai'` rng stream); and `applyFeedbackDamage`/`staggerStand`, the two damage-routing halves of GDD §3.1 (a hit on the Stand transfers to the User's HP at `getFeedbackRate`; a hit on the User staggers the Stand for `STAND_STAGGER_FRAMES` = 24, forcing an immediate Project release).
  - `resolvers.js` gained `resolveTetherLength`/`resolveFeedbackRate` (tech §2.1's minimum surface additions `getTetherLength`/`getFeedbackRate`, `hooks.js`), wrapping `stats.js`'s Phase-3 `resolveTetherPx`/`resolveFeedbackPct` the same way `resolveMoveFrames` wraps `getMoveFrames` — the two stats Phase 3 computed but nothing consumed ("Do not... build the tether" was explicitly out of that phase's scope) are now live. `resolveDamage` also gained the Strain damage penalty (`STRAIN_DAMAGE_MULT` = 0.8, read off `ctx.attacker.strained`, same pattern as the existing `defender.breakActive`/`staggered` reads) — still the single choke point, invariant 5 held exactly.
  - Damage routing (deliverable 2): all player-dealt hitboxes now originate from `combat.stand`'s transform, not the player's (`combat_player.js`'s `resolveHitboxes` passes `stand` as the attacker into `hitbox.js`'s `overlaps`/`stepMoveHitboxes`) — reach is now genuinely anchored ~22px closer to the enemy than the User, a real (not cosmetic) consequence of the Stand becoming a real body. `combat_enemy.js`'s melee/projectile hit tests go through the new target picker instead of testing only against `player`; `combat_defense.js`'s `resolveIncomingAttack(combat, pattern, atX, target)` takes the picked target and, when it's the Stand, skips the entire defensive triangle (Step/Guard/Clash/armor) and routes straight through `applyFeedbackDamage` — a deliberate design call, not an oversight: **Step/Guard/Clash protect only the User's own hurtbox**, since the whole point of the mechanic is a hurtbox the User's own defensive tech doesn't cover. A landed hit on the User (raw or guarded chip) always calls `staggerStand`.
  - Close-Range control (deliverable 4): `input.js` adds `project` (default `KeyF`, held-triggered like `guard`). `combat_player.js`: Step is denied outright while `player.projecting` (GDD §3.4 "you cannot Step"); the idle-movement branch returns early while Projecting (rooted — input is still read every frame, but by `combat_stand.js` to drive the Stand, not the User). Guard/Clash are deliberately **not** disabled while Projecting (only Step is, per the GDD's own bullet list — "fully exposed" is read as emergent from rooted + Step-less, not a fourth unstated restriction; flagged in the phase report for review).
  - Hooks (deliverable 7, all live and mutable, `hooks.js`): `onProjectStart`/`onProjectEnd` (edge-fired on the Project hold's start/end, `onProjectEnd`'s `ctx.forced` distinguishes a voluntary release from a stagger-forced one), `onTetherStrain` (fires every over-extended frame, mutable — a Fragment can waive/soften the drain-and-drag before `combat_stand.js` applies it, same shape as `onDamageIncoming`), `onFeedbackDamage` (fires on every Stand hit, before the transferred amount is applied — the hook a defensive Fragment targets).
  - Camera/aggro (deliverable 5): `render_adapter.js`'s `cameraTargetX`/`depthSort` were already generalized to N entities in Phase 1 for exactly this — no changes needed there. `render.js`'s sprite-stamping loop now explicitly skips non-fighter `kind`s (the Stand has its own dedicated `drawStand` call, positioned from the real entity transform instead of a hardcoded player-relative offset — the one place this phase's "additive only" render exception applies) so adding the Stand to `combat.entities` doesn't double-draw it.
  - HUD (deliverable 6, `hud.js`): a new row — a tether fill bar (current User↔Stand distance / `tetherPx`, turns red past 100%), `FEEDBACK NN%`, and a pulsing red `STRAIN` label that only appears while `player.strained`. `arena.js`'s new `tetherLine` draws a thin line between User and Stand, but only while Projected or Strained, so it doesn't clutter the default anchored view.
  - Known gap, flagged not silently resolved: `pose_player.js` (off-limits, no explicit phase requirement to add a pose) has no dedicated Project pose, so the Stand sprite itself only manifests when `pose.standOut > 0` (i.e. mid-attack/parry/victory, unchanged since before this phase) — holding Project while otherwise idle is visually legible only through the HUD readout and `tetherLine`, not through the Stand sprite visibly reaching outward. `drawBarrage`'s Rush fist-flurry visual still anchors to the User's own position rather than the Stand's real transform (a minor, pre-existing visual seam, not worth the diff to chase this phase).
  - Bug found and fixed in passing (pre-existing, Phase 3, not this phase's scope): `fairness_check.js` referenced the Node-only `process` global at module top level; since Phase 3 wired it into the live runtime (`content_registry.js` → `combat.js` → `index.js`), the app has been unplayable in an actual browser (not just headless) since that phase. Guarded with `typeof process !== 'undefined'`; the CLI-standalone usage (`node apps/standbattle/fairness_check.js`) is unchanged.
  - Verified via `headless_harness.js` (still byte-identical across two runs with the same seed) plus scripted headless scenarios exercising Project/Strain/snap-back/Step-denial/feedback-routing/Stand-stagger, and a real browser session (a minimal harness mounting the app directly, since the desktop shell's `kernel/boot.js` currently 404s on several unrelated, pre-existing-missing kernel files — `drunk.js`/`music.js`/`mixer.js`/`panic.js`/`god.js`/`holyc.js` — outside this app's scope to fix). A throwaway Fragment-shaped query registration on `getTetherLength`/`getFeedbackRate` (installed and removed the same way `data.js`'s `RUN_BUFFS` already do via `installRunBuffs`) proved both are live, mutable, data-addressable hooks with zero engine edits, per deliverable 8, then was deleted.
  **Crowd combat (GDD §4.1/§4.2/§4.4/§16, Phase 5):** encounters can now hold N enemies that pressure the player fairly and legibly, instead of exactly one.
  - `encounter.js` — waves, spawn scheduling and win-condition data (deliverable 1). `winCondition: 'killAll'` is the only implementation today, looked up in a `WIN_CONDITIONS` registry so Rule Fights and GDD §15's encounter objectives are future entries in that same lookup, never a rewrite of how a fight ends. A legacy single enemy/boss def (no `.waves`) is normalized (`normalizeEncounter`) into a trivial one-wave encounter — **a boss/elite/solo fight is just an N=1 crowd, not a separate code path**, which is why `combat.tokenSystem`/`combat_crowd.js` run over every fight unconditionally and are a no-op for one. `stepEncounter` spawns wave 0 synchronously at fight creation (so a solo fight's enemy exists on frame 0 exactly as before), then telegraphs later waves 90 frames (1.5s, GDD §4.4) before spawning them once the current wave is fully cleared, and only checks the win condition once every wave has actually spawned (checking earlier would misread "wave 1 cleared, wave 2 not spawned yet" as victory). `encounter_budget.js`'s `generateEncounterBudget(rng, budget, pool)` is the composition generator (deliverable 6): generic over whatever `cost`/`ranged`/`role`/`clashable` fields an `ENEMIES` entry carries (never over an enemy id by name) — never >2 ranged, never a Shielder without pressure already on the field, ≤1 Caller, and a fairness-floor repair pass that swaps in a Clash-able pick if the draw somehow has none. `encounter_check.js` (`node apps/standbattle/encounter_check.js`, same standalone-regression pattern as `fairness_check.js`/`content_check.js`) sweeps 500 random budgets asserting all four rules hold.
  - `token.js` — the attack-token rule (deliverable 2, GDD §16): the encounter holds 2 melee token slots plus 1 reserved-but-unexercised ranged slot (`TOKEN_MELEE_COUNT`/`TOKEN_RANGED_COUNT`, `combat.js`; a future Menace *Crowded* condition bumps the count, not implemented yet). Only a token holder may leave `ai.js`'s `'approach'` state; a token is held for one full pattern (windup→active→recover) and then goes on a 36–72-frame (0.6–1.2s) cooldown before reassignment (`stepTokens`, called every sim frame before any enemy's AI steps), so pressure arrives in readable pulses instead of every free slot recommitting instantly. Assignment is a weighted random pick (`profiles.js`'s `scoreForToken`, on the deterministic `'ai'` rng stream) biased toward a candidate already within its own pattern's engageable range ("a clean line") and toward whichever side of the player is *not* currently faced (GDD §16's own wording) — never a hard gate, so no profile can starve outright, only shift the odds.
  - `profiles.js` — behaviour profiles as data (deliverable 3): `aggressor`/`spacer`/`turtle`/`flanker`/`opportunist`/`support`, each a token-usage *weight*, not a bespoke state machine — extends the existing `PATTERNS` library's selection logic rather than replacing it, per the mission brief. Only `aggressor` (Delinquent), `opportunist` (Knife Thug — scores much higher for a token while the player is mid-recovery/hitstun/staggered, "punishes greed") and `turtle` (Brute — scores lower, rarely volunteers) are exercised by Phase 5's roster; `spacer`/`flanker`/`support` are real and ready for the Zoner/Hound/Puppeteer types GDD §4.2 reserves them for.
  - Off-token enemies still matter (deliverable 4) with zero new code: `ai.js`'s existing `'approach'` branch already closes distance and then holds position once in range, so a token-less enemy visibly "holds spacing" rather than freezing; they remain fully poise-staggerable and hittable (`combat_player.js`'s hitbox resolution — see below — was never gated by tokens, only the *enemy's own* ability to attack was).
  - Three enemy types as pure data (deliverable 5, `data.js`'s `ENEMIES`): **Delinquent** (`morioh_thug`, already shipped — gained `profile: 'aggressor'`, `cost: 2`, `clashable: true`, no stat changes). **Knife Thug** (new — hp 22, `profile: 'opportunist'`, a new `quick_stab` pattern in `ai.js`, 17-frame/283ms windup, the shortest in the roster but still clear of the 260ms fairness floor with margin). **Brute** (new — hp 95, poise 60, `profile: 'turtle'`, exactly one pattern, the existing armored `telegraphed_slam` — "must be respected" is the whole kit). Both new types get a `tint` (reusing the existing `MODIFIERS`-style tint mechanism, applied in `encounter.js`'s `spawnWave`) so they read apart from the Delinquent's silhouette at a glance with zero new sprite art.
  - The engine's two real generality leaks, reported as asked: **(1)** `ai.js`'s `stepEnemyAI` needed exactly one new parameter (`canCommit`, gating only the `'approach'`→`'windup'` transition) — `PATTERNS`, `pickPattern`, `defaultApproachRange`, `enterStagger` and `enemyIsVulnerableToStagger` needed zero changes. **(2)** `hitbox.js`'s `stepMoveHitboxes` needed a real signature change — `defender` (one) → `defenders` (array) with `spent` becoming a `Map<hitboxIndex, Set<defender>>` instead of a `Set<hitboxIndex>` — so one wide swing can connect with several crowd enemies in a single activation, each hit only once per window, the standard beat-'em-up rule. `combat_player.js`'s `resolveHitboxes` now tests every living enemy instead of a fixed `combat.enemy`.
  - Attacker/target generality: `combat_defense.js`'s `resolveIncomingAttack`/`applyIncomingDamage` and `combat_stand.js`'s `applyFeedbackDamage` now take an explicit `attacker` parameter (the specific crowd enemy whose pattern/projectile landed) instead of reading the fixed `combat.enemy`; `combat_enemy.js`'s `stepEnemyMovementAndAI`/`updateEnemyPhase` take the specific `enemy` being stepped (called once per entry in `combat.enemies` from the new `combat_crowd.js`, which also drives `token.js`/`encounter.js` each frame, in that order — tokens before AI so `enemy.hasToken` is fresh, encounter-stepping last so a kill landed this frame is caught the same frame). `combat.enemy` survives as a compat alias to `enemies[0]`, read only by boss/solo-only code that never sees more than one enemy (`sprite_boss.js` via `render.js`, `hud.js`'s detailed solo panel, `index.js`'s tense-music check) — an elite node gaining an escort therefore demotes its HUD to the new crowd panel automatically, a deliberate consequence of "solo is just an N=1 crowd," not a bug. Player facing and Step's dodge direction now point at the *nearest* living enemy (`combat_player.js`'s `nearestAliveEnemy`) rather than a fixed one.
  - Render/HUD (additive, this phase's explicit exception to render.js being otherwise off-limits): `render.js`'s `drawCombat` computes one pose per living-or-dying enemy and depth-sorts/stamps all of them; `arena.js`'s `telegraph`/`projectiles` loop over `combat.enemies` (an off-token enemy simply never reaches `telegraph`'s `'windup'`-only early-out, which is what makes "who's about to commit" legible with **zero new HUD chrome** — the acceptance criterion this phase cares about); `hud.js` keeps its exact pre-Phase-5 detailed single-enemy portrait/bar/boss-phase-pips panel whenever exactly one enemy is alive, and only switches to a new compact multi-row name+bar list (reusing the same `bar()`/`text()` primitives, no new art) once there's more than one; `debug_overlay.js` loops every living enemy's hurtbox/hitbox and adds a `tok:Y/n` marker per enemy stat line for developer verification of the token gate. `fx.js`'s `onHit` handler now reads a new `target` field on the `onHit` payload (`combat_player.js`) so impact fx land on the actual enemy hit rather than always anchoring near `enemies[0]`.
  - Act I wiring: node `n3` ("SHOPPING STREET") is now `ENCOUNTERS.morioh_shopping_street` — wave 1 is generator-composed (`generateEncounterBudget`, budget 5, pool `[morioh_thug, knife_thug]`) and wave 2 is a single telegraphed Brute; node `n5` ("BUDOGAOKA PARK", elite) is `ENCOUNTERS.budogaoka_park_elite` — Angelo plus a Delinquent escort in one wave, GDD §5.3's "Elite = affixed enemy + escort" read literally. `index.js`'s `startCombatForNode` resolves `node.encounter` through the new `ENCOUNTERS` map alongside the existing `node.enemy`/boss paths.
  - Verified via `headless_harness.js` (extended with an optional `encounter` param, still byte-identical across two runs of the same seed — crowd determinism holds exactly like the solo case always has) plus dedicated headless scripts confirming the token cap actually holds (never more than 2 of N enemies simultaneously in `'windup'`/`'active'` across hundreds of frames and multiple seeds) and that both new wiring paths (n3's waves, n5's elite+escort) spawn/telegraph/resolve without error; and a real browser session (the same minimal-mount-harness technique the Phase 4 report used, since `kernel/boot.js` is still missing the same unrelated files) clicking through title→n1→n2→n3 with zero console/page errors, with a debug-overlay screenshot catching the token gate live mid-fight (one enemy `TOK:Y` mid-`quick_stab`, the other `TOK:N` holding position).
  - Known gaps, flagged not silently resolved: the token cooldown window (36–72f) and the two shipped encounters' budgets are first-pass numbers that only prove the *mechanism* — a deliberately non-defensive scripted headless attacker reliably loses to n3's crowd, which is expected of a bot that never Steps/Guards/Clashes, but no human playtest was available in this environment to confirm the encounter is actually *fun* rather than merely fair; flagged for a balance pass. The reserved ranged token pool (`tokenPool: 'ranged'`) is real and wired but has never been exercised by real content, since none of Phase 5's three shipped types are ranged. `fx.js`'s unused, pre-existing `mid()` helper (a screen-center approximation for full-screen effects) still reads `combat.enemy` rather than any real target — harmless since it was already dead code before this phase, left as-is rather than chased down.
  **Cross-cutting foundations (tech-doc Phase 0):** four modules every later phase assumes.
  - `rng.js` — one xorshift128 PRNG seeded per run (`createRng(seed)`), with derived named sub-streams (`rng.stream('map'|'rewards'|'combat'|'ai')`) so drawing from one stream never perturbs another. `index.js` creates the run's `rng` from `runState.seed` and threads it into `createCombat(enemyDef, buffs, opts, rng)`; `combat.js` pulls `rng.stream('ai')` and passes it into `ai.js`'s `pickPattern`/`stepEnemyAI`, replacing their `Math.random()` calls — enemy pattern choice is now reproducible from the run seed. The `'map'`/`'combat'` streams are reserved, unused until map generation and frame-data land. Render/particle randomness (`juice.js`, `fx.js`, `arena.js`, and the still-untouched rendering-pipeline files) is deliberately left on plain `Math.random()`/existing seeds — already a separate, unseeded generator per the invariant, so a dropped frame can never desync the sim; `rng.js` also exports `createUnseededRng()` for any future code that wants that same guarantee explicitly.
  - `save.js` — the single choke point over `ctx.save`/`ctx.load`. Two independent blobs, `'run'` and `'meta'`, each `{ version, data }` through a per-blob migration table (both v1, no-op tables so far). `index.js` persists `runState` (`seed, hp, maxHp, nodeIndex, buffs`) at every node-boundary transition (new run, node clear, event/rest resolution) and clears it on run loss or completion; on `mount`, an existing run blob resumes straight to the map scene instead of the title, so killing the tab mid-run loses at most the in-progress node. `meta` (`shakeEnabled`, `cleared`, `keymap`) is loaded/saved independently, so a corrupted run blob can never take meta down with it.
  - `constants.js` — re-exports the arena bounds from `arena_bounds.js` (now including the Phase 1 depth bounds `WORLD_D`/`ARENA_Z_MIN`/`ARENA_Z_MAX`/`Z_REST`) plus every other number that must stay identical between sim and render: `SIM_HZ`/`FRAME_MS` (the sim's fixed 60Hz step, actually used since Phase 1 -- see below), `GROUND_Y` (one source, previously redeclared as the literal `208` independently in both `render.js` and `arena.js`), and `DEATH_ANIM_FRAMES` (shared between combat.js setting it and pose_enemy.js reading it back).
  - `input.js` — owns a rebindable keymap (persisted through `save.js`'s `meta` blob, no rebind UI yet) and explicitly classifies every action as edge-triggered (`EDGE_ACTIONS`: light/medium/heavy/special/rush/dodge/parry) or held-triggered (`HELD_ACTIONS`: left/right, plus Phase 1's forward/back for the z axis, W/S by default), plus a capped ring buffer of `(action, frame)` for future debugging/replay. `index.js`'s key handler now routes through `input.resolveKey(code, down)` before forwarding the same down/up transitions to `combat.setKey` exactly as before — this is bookkeeping layered on top, not a new gate, so combat feel is unchanged.
  **Graphics (rebuilt from scratch, 480×270 internal):** every pixel is generated by a software rasterizer (`draw.js`) that only ever emits axis-aligned 1px rows — canvas antialiases both path fills and rotated `fillRect`s, so nothing in this app calls `fill()`/`stroke()`/`ctx.rotate` for art. On top of that:
  - `palette.js` — five-step colour ramps (core shadow → shadow → base → light → rim) with hue-shifted shadows, one shared light direction, and `haze()` for atmospheric perspective.
  - `layer.js` — offscreen sprite compositor: pooled buffers, silhouette-traced ink outlines, character-shaped cast shadows, hit flashes, dodge afterimages, squash/stretch, plus `cached()` for painted-once background layers.
  - `body.js`/`face.js` — shared humanoid rig (FK from joint angles, cel-shaded limbs, hands, boots) and anime-style heads (lidded eyes with iris + specular, expression brows, mouths).
  - `anim.js` + `pose_player.js`/`pose_enemy.js` — pose engine: anticipation/overshoot easing per action, spring-lagged hair and coat driven by body velocity, breathing/blinking/weight-shift idles, per-pattern enemy telegraph shapes, and flinch layered on top of whatever the enemy was already doing.
  - `sprite_jotaro.js`, `sprite_star.js` (Star Platinum, incl. the fists-and-streaks barrage), `sprite_enemy.js`, `sprite_boss.js` — ~100–120px characters, roughly 5× the pixel detail of the previous pass.
  - `background.js` + `bg_scenes.js` + `bg_props.js` — six parallax layers per arena (sky, far ridge, mid town, near town, ground, foreground silhouettes) across four Morioh locations, cached to buffers.
  - `font.js`/`font_data.js` — 5×7 bitmap font; no antialiased system text anywhere.
  - `fx.js` — impact stars, manga speed lines, swing arcs, charge motes, shockwaves, onomatopoeia, dust and screen flashes, spawned from the effect-hook dispatcher rather than from the combat loop; screen-wide effects are `solo` so a fast combo can't stack four flashes into a whiteout.
  - `arena.js` — attack telegraphs, projectiles, particles and the render-only reactions (swing arcs, footfall dust, stagger scuffs) that watch combat state without the simulation knowing they exist.
  - `render.js` — camera, parallax, sprite stamping (outline + cast shadow + scene rim light + squash), HUD.
  This deliberately goes beyond the machine's base 16-colour/no-antialiasing rule below — an explicit exception for this app's content, requested by the user; canvas smoothing stays off and everything still snaps to whole pixels. Sound is a real 3-layer SFX design with combo-pitch escalation (`audio.js`) plus a from-scratch adaptive chiptune engine (`music.js`, its own Web Audio gain bus wired to the machine's MUS knob, independent of the SFX bus) with explore/combat/tension intensity layers.
  **Design documents — read before changing gameplay:**
  - `docs/stand-battle-arena-spec.md` — the technical contract (architecture, pixel pipeline, audio, fairness constraints). Binding for architecture and rendering.
  - `docs/stand-battle-arena-gdd.md` — the game design document: the User/Stand duality that is the core mechanic, the three Stand Classes, run structure, the six item layers (Fragments / Requiem / Relics / Discs / Aspects / economy), difficulty axes, meta-progression, and the 40-hour content math. Binding for gameplay.
  - `docs/stand-battle-arena-tech.md` — audit of the shipped prototype, the engine systems the GDD requires (effect pipeline, stat pipeline, frame data, 2.5D arena, seeded RNG, content validator), data schemas and build order.
  There is **no line budget** on this project — the engine may be as large as the design needs. The constraint is generality: content is data, never a new code path.

## Rules
- Apps never import from `kernel/`.
- Files stay under 300 lines (split into siblings in the app folder if needed).
