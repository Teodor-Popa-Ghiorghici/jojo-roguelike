/* Duo Fragments — extended batch (Phase 10's 5 new donors' remaining
   pairings, including Hermit Purple's "+ anything -> Fated" wildcard
   family from GDD §6.1). Same schema as core.js. */

export const DUO_FRAGMENTS_EXTENDED = [];
