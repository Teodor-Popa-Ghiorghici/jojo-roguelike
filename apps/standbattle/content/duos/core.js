/* Duo Fragments — core batch (the 3 Phase 7 donors' pairings + their
   pairings with the first wave of Phase 10 donors). GDD §6.1: "offered
   only when you hold Fragments from two specific donors"; tech §3's
   schema: { id, name, rarity, requires:[{donor},{donor}], desc,
   effects[], queries[] } -- no slot, no levels. */

export const DUO_FRAGMENTS_CORE = [];
