/* Relics — Power/Risk batch. GDD §6.3: persistent, run-wide, no slot;
   every downside is stated at pickup, never a hidden clause (tech §3's
   Stone Mask example is the model: real hook, real tradeoff). Canon
   power-for-cost objects (Stone Mask, Aja Stone-adjacent stat trades). */

export const RELICS_POWER_RISK = [];
